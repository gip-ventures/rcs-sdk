/**
 * Global type declarations for the RCS SDK
 */

// Declare any global types here that are not covered by TypeScript's built-in types
// or types from dependencies

// Example:
// declare module 'some-untyped-module' {
//   export function someFunction(): void;
// }